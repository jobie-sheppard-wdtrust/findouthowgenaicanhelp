<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GenAI Chatbot</title>
  <style>
    * { box-sizing: border-box; }

    :root {
      --background: hsl(220 20% 97%);
      --foreground: hsl(220 20% 10%);
      --card: hsl(0 0% 100%);
      --border: hsl(220 15% 90%);
      --primary: hsl(221 83% 53%);
      --primary-foreground: hsl(0 0% 100%);
      --muted-foreground: hsl(220 10% 42%);
      --chat-bot: hsl(220 15% 95%);
      --chat-bot-foreground: hsl(220 20% 15%);
      --focus-ring: hsl(221 83% 45%);
    }

    html, body {
      margin: 0;
      min-height: 100%;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      line-height: 1.5;
      color: var(--foreground);
      background: var(--background);
    }

    .app {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      flex-shrink: 0;
      padding: 1rem 1.5rem;
      border-bottom: 1px solid var(--border);
      background: rgb(255 255 255 / 0.55);
      backdrop-filter: blur(8px);
    }

    .inner {
      max-width: 48rem;
      margin: 0 auto;
      width: 100%;
    }

    h1 {
      margin: 0;
      font-size: 1.125rem;
      font-weight: 700;
    }

    .subtitle {
      margin: 0.2rem 0 0;
      font-size: 0.875rem;
      color: var(--muted-foreground);
    }

    main {
      flex: 1;
      overflow: auto;
      scroll-behavior: smooth;
      padding: 1.5rem 1rem;
    }

    .transcript {
      max-width: 48rem;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      border-radius: 0.5rem;
    }

    .message {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .avatar {
      width: 2.25rem;
      height: 2.25rem;
      border-radius: 9999px;
      background: #90278e;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .avatar svg {
      width: 1.25rem;
      height: 1.25rem;
      stroke: white;
      stroke-width: 2;
      fill: none;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .bubble {
      max-width: 32rem;
      background: var(--chat-bot);
      color: var(--chat-bot-foreground);
      border-radius: 1rem;
      border-top-left-radius: 0.375rem;
      box-shadow: 0 2px 8px rgb(0 0 0 / 0.08);
      padding: 0.75rem 1rem;
      font-size: 0.9375rem;
      overflow-wrap: anywhere;
    }

    footer {
      flex-shrink: 0;
      padding: 0.5rem 1rem 1.5rem;
      background: linear-gradient(to top, var(--background), var(--background), transparent);
    }

    .footer-inner {
      max-width: 48rem;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .prompts {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      justify-content: center;
    }

    .prompt-btn {
      padding: 0.5rem 1rem;
      border: 1px solid var(--border);
      border-radius: 9999px;
      font-size: 0.875rem;
      color: var(--foreground);
      background: var(--card);
      cursor: pointer;
      min-height: 2rem;
    }

    .prompt-btn:hover {
      background: var(--primary);
      color: var(--primary-foreground);
      border-color: var(--primary);
    }

    .instruction {
      margin: 0;
      font-size: 0.875rem;
      color: var(--foreground);
    }

    .input-wrapper {
      position: relative;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 1rem;
      box-shadow: 0 4px 12px rgb(0 0 0 / 0.08);
    }

    textarea {
      width: 100%;
      min-height: 3.5rem;
      max-height: 12rem;
      resize: vertical;
      padding: 1rem 3.5rem 1rem 1.25rem;
      border: none;
      background: transparent;
      color: var(--foreground);
      font: inherit;
      font-size: 0.9375rem;
    }

    textarea::placeholder { color: var(--muted-foreground); }
    textarea:focus { outline: none; }

    .send-btn {
      position: absolute;
      right: 0.75rem;
      top: 50%;
      transform: translateY(-50%);
      width: 2.5rem;
      height: 2.5rem;
      border-radius: 0.75rem;
      border: none;
      background: var(--primary);
      color: var(--primary-foreground);
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }

    .send-btn:disabled {
      opacity: 0.4;
      cursor: not-allowed;
    }

    .send-btn svg {
      width: 1.25rem;
      height: 1.25rem;
      stroke: currentColor;
      fill: none;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    :focus-visible {
      outline: 3px solid var(--focus-ring);
      outline-offset: 2px;
    }

    .visually-hidden {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
  </style>
</head>
<body>
  <div class="app">
    <header>
      <div class="inner">
        <h1>GenAI Chatbot</h1>
        <p class="subtitle">See some examples of the help it can provide</p>
      </div>
    </header>

    <main id="chat-main" tabindex="0" aria-label="Conversation transcript">
      <div id="transcript" class="transcript" role="log" aria-live="polite" aria-relevant="additions text" aria-atomic="false">
        <article class="message">
          <div class="avatar" aria-hidden="true">
            <svg viewBox="0 0 24 24" focusable="false" aria-hidden="true">
              <path d="M12 8V4H8"></path>
              <rect width="16" height="12" x="4" y="8" rx="2"></rect>
              <path d="M2 14h2"></path>
              <path d="M20 14h2"></path>
              <path d="M15 13v2"></path>
              <path d="M9 13v2"></path>
            </svg>
          </div>
          <div class="bubble">
            <span class="visually-hidden">Bot: </span>
            <span>How can I help?</span>
          </div>
        </article>
      </div>
    </main>

    <footer>
      <div class="footer-inner">
        <div class="prompts" role="group" aria-label="Preset prompts">
          <button type="button" class="prompt-btn">Research NICE Sepsis recommendations</button>
          <button type="button" class="prompt-btn">Draft a response to a patient complaint</button>
          <button type="button" class="prompt-btn">What are arguments for and against my colleague's discharge plan?</button>
        </div>

        <div class="input-wrapper">
          <label for="message-input" class="visually-hidden">Message</label>
          <textarea id="message-input" name="message" rows="1" placeholder="Click or activate one of the preset prompts..." aria-describedby="message-instruction"></textarea>
          <button id="send-btn" class="send-btn" type="button" aria-label="Send" disabled>
            <svg viewBox="0 0 24 24" focusable="false" aria-hidden="true">
              <line x1="22" y1="2" x2="11" y2="13"></line>
              <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
            </svg>
          </button>
        </div>
      </div>
    </footer>
  </div>

  <script>
    const messageInput = document.getElementById('message-input');
    const sendButton = document.getElementById('send-btn');

    function updateSendState() {
      sendButton.disabled = messageInput.value.trim().length === 0;
    }

    messageInput.addEventListener('input', updateSendState);
    updateSendState();
  </script>
</body>
</html>
